# DISP-LIVINGWORLD-builder

- srs: SRS-CN-BUILDER-LIVINGWORLD-001
- guild: builder
- champion: Forge
- branch: bits/livingworld
- status: queued
- brief: .bits/srs/SRS-CN-BUILDER-LIVINGWORLD-001.md

Open a code session on `bits/livingworld` and build **SRS-CN-BUILDER-LIVINGWORLD-001** — the builder guild's public living-world floor contract (the `citadel.builder.*` events + `/realm/builder.json` feed the Aincrad game reads). Read the brief above + this repo's `.bits/context.md`. Public-safe only. Self-gate `npm run lint` + `npm test`. Open a PR titled after the SRS.
